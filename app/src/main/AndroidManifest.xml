<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodhelp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <!--
     Not working
      <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodHelp">
        <activity
            android:name=".register_Note"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Plate Saver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_shelter_form"
            android:exported="true" />
        <activity
            android:name=".activity_rest_form"
            android:exported="true" />
        <activity
            android:name=".activity_rest_confirmation"
            android:exported="true" />
        <activity
            android:name=".activity_registeration_form"
            android:exported="true" />
        <activity
            android:name=".activity_login_reg"
            android:exported="true" />
        <activity
            android:name=".activity_login_form"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBg-lCqwPp-wyTj75sImRiiteZ5g2APRd4" />

        <activity
            android:name=".MapsActivity"
            android:exported="true"></activity>
    </application>

</manifest>